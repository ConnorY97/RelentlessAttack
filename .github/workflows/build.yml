name: Building on self hosted runner

on:
  workflow_dispatch:
    {}

jobs:
  testing:
    name: Build  and post project
    runs-on: self-hosted  # Use the self-hosted runner

    steps:
        - name: Checkout repo
          uses: actions/checkout@v4
          with:
            lfs: false

        - name: Build
          shell: bash
          run: |
            ~/Unity/Hub/Editor/2023.2.18f1/Editor/Unity \
            -quit -batchmode -nographics -buildTarget \
            EmbeddedLinux -executeMethod Builder.Build \
            -projectPath /home/connor/Documents/RelentlessAttack/